[project]
name = "bit-case-generator"
version = "0.1.0"
description = "build123d でドライバービット収納ケースを生成"
requires-python = ">=3.11"
dependencies = [
    "build123d>=0.7.0",
]

[tool.uv]
dev-dependencies = []
